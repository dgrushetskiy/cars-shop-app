<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog logicalFilePath="db/changelog/changelog.xml"
                   xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- schema for liquibase -->
    <changeSet id="create-scheme-liquibase_schema" author="dg">
        <sql>
            CREATE SCHEMA IF NOT EXISTS liquibase_schema AUTHORIZATION root;
        </sql>
    </changeSet>
    <!-- schema for cars -->
    <changeSet id="create-scheme-cars" author="dg">
        <sql>
            CREATE SCHEMA IF NOT EXISTS cars AUTHORIZATION root;
        </sql>
    </changeSet>
    <!-- schema for test-db -->
    <changeSet id="create-scheme-test-db" author="dg">
        <sql>
            CREATE SCHEMA IF NOT EXISTS test_db AUTHORIZATION root;
        </sql>
    </changeSet>
    <!-- schema for batch -->
    <changeSet id="create-scheme-batch" author="dg">
        <sql>
            CREATE SCHEMA IF NOT EXISTS batch_schema AUTHORIZATION root;
        </sql>
    </changeSet>
</databaseChangeLog>
